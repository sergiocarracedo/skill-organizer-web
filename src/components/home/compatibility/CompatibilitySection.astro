---
import AgentToolLogo from '@/components/AgentToolLogo.astro'
import { compatibilityItems } from '@/data/home'
---

<section class='compatibility-section'>
  <div class='compatibility-section__copy' data-reveal>
    <p class='compatibility-section__eyebrow'>Compatibility</p>
    <h2 class='compatibility-section__title'>Works with the tools you already use.</h2>
    <p class='compatibility-section__body'>
      Keep one organized source tree and generate the flat target expected by Claude Code, OpenCode, Codex,
      Cursor, and Antigravity.
    </p>
  </div>

  <div class='compatibility-section__supports' data-reveal>
    <span class='compatibility-section__supports-label'>Works with</span>
    <div class='compatibility-section__supports-strip'>
      {compatibilityItems.map((item) => (
        <AgentToolLogo id={item.id} name={item.name} compact iconOnly />
      ))}
    </div>
  </div>
</section>

<style>
  .compatibility-section {
    display: grid;
    max-width: 84rem;
    margin: 0 auto;
    padding: 6rem 1.5rem 0;
    gap: 1.5rem;
    align-items: end;
  }

  .compatibility-section__copy {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .compatibility-section__eyebrow {
    font-family: var(--font-display);
    font-size: 0.76rem;
    letter-spacing: 0.3em;
    text-transform: uppercase;
    color: rgba(177, 225, 255, 0.72);
  }

  .compatibility-section__title {
    max-width: 18ch;
    color: white;
    font-size: clamp(2.1rem, 5vw, 3.5rem);
  }

  .compatibility-section__body {
    max-width: 56rem;
    font-size: 1.06rem;
    line-height: 1.88;
    color: rgba(235, 244, 255, 0.78);
  }

  .compatibility-section__supports {
    display: flex;
    flex-direction: column;
    align-items: flex-start;
    gap: 0.8rem;
    padding-top: 0.5rem;
  }

  .compatibility-section__supports-label {
    font-family: var(--font-display);
    font-size: 0.78rem;
    letter-spacing: 0.22em;
    text-transform: uppercase;
    color: rgba(177, 225, 255, 0.68);
  }

  .compatibility-section__supports-strip {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 0.4rem;
  }

  @media (min-width: 960px) {
    .compatibility-section {
      padding: 7rem 2rem 0;
      grid-template-columns: minmax(0, 1.05fr) minmax(16rem, 0.95fr);
    }
  }
</style>
